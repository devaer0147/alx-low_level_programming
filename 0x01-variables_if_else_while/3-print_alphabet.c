#include <stdio.h>

/**
 * main - Entry point
 * lowercase and uppercase alphABET
 * Return: Always 0 (Success);
*/
int main(void)
{
	char a;

	for ( a = 'a'; a <= 'Z'; a++)
	{
		putchar(a);
	}
	putchar('\n');
}
