#include "main.h"

/**
 * _isdigit - check if isdigit
 * @c: operand to be checked
 *
 * Return: 1 if c is digit and 0 if otherwise
 * Description: function to check if c is digit or not
 */
int _isdigit(int c)
{
	if (isdigit(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);

}
