#include "main.h"

/**
 * add - returns the sum of two given
 * intergs
 * @a: first parameter
 * @b: second parameter
 *
 * Return: int
 */
int add(int a, int b)
{
	int add;

	add = (a + b);
	return (add);
}
